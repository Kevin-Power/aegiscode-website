# Public marketing PDFs served straight from the CDN. Committed to git
# (see .gitignore policy comment) and regenerated by
# `npm run regenerate-pdfs` (see docs/PDF_REGENERATION.md).
#
# Files expected here:
#   - tw-compliance-matrix.pdf    (台灣資安法規對應一覽, public)
